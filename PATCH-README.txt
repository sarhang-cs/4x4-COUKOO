4X4 COUKOO v1.13.3 patch-only update

Apply this ZIP over an existing v1.13.2 project/repository.
It only replaces the files contained in this archive. It never removes game assets.

Key fixes:
- Safe confirmed selection for Low / Medium / High.
- Safe confirmed selection for frame-rate and shadows, with reload instead of a live renderer mutation.
- Medium default on a fresh install.
- Browser capability profile: logical CPU cores, reported RAM, GPU/WebGL, screen, browser storage and browser identity.
- Better High/Medium distance clarity and visibility.
- Low mobile stability path for black flashing.
- PWA cache update v1.13.3.
