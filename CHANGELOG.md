# Changelog

## v1.14.2 — README and build-warning cleanup

- Restored the richer README layout with screenshots.
- Updated README release notes to match the current stabilized runtime.
- Kept the README image references inside `static/readme/`.
- Raised Vite `chunkSizeWarningLimit` to cover the validated engine chunk so production builds complete without the previous chunk-size warning.
